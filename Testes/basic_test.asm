 MOV R0, #10
 MOV R1, #20
 ADD R2, R0, R1
 HALT

        MOV R0, #10      ; R0 = 10
        MOV R1, #20      ; R1 = 20
        ADD R2, R0, R1   ; R2 = R0 + R1 = 30
        HALT             ; Parar execução
